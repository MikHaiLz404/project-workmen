{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Profile",
  "description": "The Workmen profile contract. See module docs for field semantics.\n\nThe `schema_version` field is deserialized through a custom function that enforces the supported version. Unknown versions fail at deserialization time (rather than at first use) so a stale cache or stale in-memory copy never silently disagrees with the on-disk file.",
  "type": "object",
  "required": [
    "budgets",
    "exceptions",
    "id",
    "matchers",
    "namingRules",
    "profileRevision",
    "schemaVersion",
    "sourceRuntime",
    "state"
  ],
  "properties": {
    "budgets": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/PlatformBudget"
      }
    },
    "exceptions": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/ProfileException"
      }
    },
    "id": {
      "type": "string"
    },
    "matchers": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/ProfileMatcher"
      }
    },
    "namingRules": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/NamingRule"
      }
    },
    "profileRevision": {
      "type": "integer",
      "format": "uint32",
      "minimum": 0.0
    },
    "schemaVersion": {
      "type": "integer",
      "format": "uint32",
      "minimum": 0.0
    },
    "sourceRuntime": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/SourceRuntimeRelationship"
      }
    },
    "state": {
      "$ref": "#/definitions/ProfileState"
    }
  },
  "definitions": {
    "AlphaPolicy": {
      "description": "Alpha channel policy for a given platform budget.",
      "type": "string",
      "enum": [
        "required",
        "optional",
        "forbidden"
      ]
    },
    "AssetRole": {
      "description": "The six-role vocabulary for asset classification.\n\nRoles are deliberately distinct, even when some have empty user populations today. Downstream tasks add new evidence for `Derived` and `Mirror Target`; modelling them now keeps the on-disk schema stable.",
      "oneOf": [
        {
          "description": "The original, hand-authored art for an asset family.",
          "type": "string",
          "enum": [
            "source"
          ]
        },
        {
          "description": "The build output that ships with the game.",
          "type": "string",
          "enum": [
            "runtime"
          ]
        },
        {
          "description": "Anything generated from a Source by Workmen (previews, mirrored copies, packed atlases, ...).",
          "type": "string",
          "enum": [
            "derived"
          ]
        },
        {
          "description": "A copy of a Runtime asset created for a specific target platform (web/iOS/Android) by a separate export pipeline. Always linked back to one Runtime asset.",
          "type": "string",
          "enum": [
            "mirrorTarget"
          ]
        },
        {
          "description": "An asset that exists but is intentionally out of scope for the active profile (deprecated, rejected, license-blocked, ...).",
          "type": "string",
          "enum": [
            "excluded"
          ]
        },
        {
          "description": "A scanned file that we could not classify with the available evidence. Held in the Unclassified Queue until the user resolves it.",
          "type": "string",
          "enum": [
            "unclassified"
          ]
        }
      ]
    },
    "Compression": {
      "description": "Compression codec negotiated for the runtime asset on a platform.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "none",
            "png"
          ]
        },
        {
          "description": "WebP. Note: serializes to `\"webP\"` (camelCase of the variant name), not the all-lowercase `\"webp\"` that some consumers may expect. This is deliberate for consistency with the other camelCase variants in this enum.",
          "type": "string",
          "enum": [
            "webP"
          ]
        }
      ]
    },
    "NamingRule": {
      "description": "A naming rule applied before raising naming violations.",
      "type": "object",
      "required": [
        "description",
        "pattern"
      ],
      "properties": {
        "description": {
          "type": "string"
        },
        "pattern": {
          "type": "string"
        }
      }
    },
    "Platform": {
      "description": "Target platform a Profile budgets for.",
      "type": "string",
      "enum": [
        "web",
        "ios",
        "android"
      ]
    },
    "PlatformBudget": {
      "description": "A platform-specific budget used by the validator.",
      "type": "object",
      "required": [
        "alphaPolicy",
        "colorSpace",
        "compression",
        "maxDecodedBytes",
        "maxEncodedBytes",
        "maxTextureHeight",
        "maxTextureWidth",
        "platform",
        "potNpot"
      ],
      "properties": {
        "alphaPolicy": {
          "$ref": "#/definitions/AlphaPolicy"
        },
        "colorSpace": {
          "description": "Free-form string describing the color space (e.g. `\"sRGB\"`).",
          "type": "string"
        },
        "compression": {
          "$ref": "#/definitions/Compression"
        },
        "maxDecodedBytes": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "maxEncodedBytes": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "maxTextureHeight": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "maxTextureWidth": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "platform": {
          "$ref": "#/definitions/Platform"
        },
        "potNpot": {
          "$ref": "#/definitions/PotNpot"
        }
      }
    },
    "PotNpot": {
      "description": "Whether the platform requires power-of-two texture dimensions.",
      "oneOf": [
        {
          "description": "Strictly power-of-two. Non-POT assets are validation errors.",
          "type": "string",
          "enum": [
            "powerOfTwo"
          ]
        },
        {
          "description": "Any dimensions are accepted.",
          "type": "string",
          "enum": [
            "any"
          ]
        }
      ]
    },
    "ProfileException": {
      "description": "A bounded, auditable exception to a Profile rule.\n\n`expires_at` is an ISO-8601 timestamp string (`chrono` is intentionally avoided for this task to keep the dependency surface small — future revisions will replace `String` with `chrono::DateTime<Utc>`).",
      "type": "object",
      "required": [
        "assetMatcher",
        "reason",
        "ruleId"
      ],
      "properties": {
        "assetMatcher": {
          "$ref": "#/definitions/ProfileMatcher"
        },
        "expiresAt": {
          "type": [
            "string",
            "null"
          ]
        },
        "reason": {
          "type": "string"
        },
        "ruleId": {
          "type": "string"
        }
      }
    },
    "ProfileMatcher": {
      "description": "A predicate that selects assets into a Profile.\n\nEach field is optional. An empty [`ProfileMatcher`] matches every asset — usually a bug, but legitimate for the \"default\" Profile.",
      "type": "object",
      "properties": {
        "assetRole": {
          "description": "Optional role filter; `None` accepts any role.",
          "anyOf": [
            {
              "$ref": "#/definitions/AssetRole"
            },
            {
              "type": "null"
            }
          ]
        },
        "extension": {
          "description": "Extension without the leading dot.",
          "type": [
            "string",
            "null"
          ]
        },
        "namingPattern": {
          "description": "Regex-style naming pattern (the syntax T5 will pin down).",
          "type": [
            "string",
            "null"
          ]
        },
        "pathGlob": {
          "description": "Project-relative glob (`.workmen`-style anchored to project root).",
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "ProfileState": {
      "description": "Lifecycle state of a [`Profile`].\n\n* `Draft` — the profile is being edited; not authoritative. * `Active` — the profile is the contract of record. * `Locked` — the profile cannot be edited; bumps require explicit unlock with a non-empty reason.",
      "type": "string",
      "enum": [
        "draft",
        "active",
        "locked"
      ]
    },
    "SourceRuntimeRelationship": {
      "description": "Declared edge between a Source asset and a Runtime asset.",
      "type": "object",
      "required": [
        "runtimePath",
        "sourcePath"
      ],
      "properties": {
        "runtimePath": {
          "type": "string"
        },
        "sourcePath": {
          "type": "string"
        }
      }
    }
  }
}
